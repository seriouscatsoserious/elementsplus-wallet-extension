# Vendored provenance

This directory is an exact export of `contrib/devtools/preconf-user-bond` from:

- repository: `https://github.com/ekulkisnek/liquid-drivechain-signet-adaptation`
- commit: `006d2a30b1df340f5d77ca9af21e1c3df18b551b`
- merged pull request: `#6`, operator-funded bond and realtime receipt relay

It is vendored so the browser verifier and signer compile against the exact reviewed wire format and digest. Update it only as an auditable whole-directory import from a named upstream commit.
