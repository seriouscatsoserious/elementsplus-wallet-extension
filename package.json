{
  "name": "elementsplus-wallet-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Security-first ECX Alpha Elements+ browser wallet scaffold",
  "scripts": {
    "build": "node scripts/build.mjs && node scripts/check-artifact.mjs",
    "typecheck": "tsc --project tsconfig.json --noEmit",
    "test": "node scripts/test.mjs",
    "test:live": "node scripts/live-network-smoke.mjs",
    "check": "npm run typecheck && npm test && npm run build"
  },
  "devDependencies": {
    "@types/node": "24.10.1",
    "typescript": "5.9.3"
  },
  "engines": {
    "node": ">=24.0.0"
  }
}
